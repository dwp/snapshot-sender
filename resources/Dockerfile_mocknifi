FROM python:3.7-slim

RUN apt-get update && apt-get install -y gcc python3-dev

RUN mkdir /ssl

WORKDIR /app

COPY mocknifi/requirements.txt .

RUN pip3 install -r requirements.txt

COPY mocknifi/* ./

CMD ["python3", "mocknifi.py"]
